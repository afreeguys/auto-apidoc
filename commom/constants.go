package commom

import "os"

const (
	ConfigPath     = "japi_Tools"
	JsonFileName   = "controller.json"
	JarFileName    = "japiTools.jar"
	ConfigFileName = "config.json"
	PathSeparator  = string(os.PathSeparator)
)
